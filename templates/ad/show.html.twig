{% extends "base.html.twig" %}

{% block body %}

    <div class="ad-title" style="background-image: url({{ ad.coverImage }})">
        <div class="container">
            <h1>{{ad.title}}</h1>

            <p>{{ad.intro}}</p>

            <p class="h2">
                <strong>{{ad.rooms}}
                    chambres</strong>
                pour
                <strong>
                    {{ad.price}}
                    &euro;
                </strong>
            </p>
            <a href="#" class="btn btn-primary">Reserver !</a>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <div class="col-8 mb-3">
                {{ad.content |raw }}

                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        {% for img in ad.images %}
                            <li data-target="#carouselExampleIndicators" data-slide-to="{{loop.index0}}" class="{% if loop.first%}active{% endif %}"></li>
                        {% endfor %}

                    </ol>
                    {{ad.images | length}}
                    <div class="carousel-inner">
                        {% for img in ad.images %}

                            <div class="carousel-item {% if loop.first %} active {% endif %}">
                                <img src="{{img.url}}" class="d-block w-100" alt="...">
                                <div class="carousel-caption d-none d-md-block">
                                    <h5>{{ad.title}}</h5>
                                    <p>{{img.caption}}</p>
                                </div>
                            </div>

                        {% endfor %}
                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="sr-only">Next</span>
                    </a>
                </div>

            </div>
            <div class="col">
                <div class="row">
                    <div class="col-4">
                        <img src="{{ad.author.picture}}" alt="phto de profil de anthony" ></div>
                    <div class="col">
                        <h3>{{ ad.author.firstName }} {{ ad.author.lastName }}
                            <span class="badge badge-primary">
                                3 annonces</span>
                        </h3>
                    </div>
                </div>
                <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Ipsum earum aspernatur asperiores quod aut alias nam nostrum possimus placeat provident?</p>
                <p>Nesciunt est non sunt hic vero error totam sed. Eius nostrum nobis placeat doloremque iusto, doloribus nulla et. Exercitationem, possimus?</p>
                <p>Soluta, quis. Iusto, itaque necessitatibus. Illo, nisi iure? Dolor fugit necessitatibus eum dolore dignissimos ullam corrupti, recusandae magni voluptates dolorem?</p>
            </div>
        </div>
    </div>

{% endblock %}
