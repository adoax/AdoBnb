{% extends "admin/base.html.twig" %}

{% block title %}Edition du profil
	{{ user.fullName }}
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="my-5">Edition du profil
			{{ user.fullName }}
		</h1>
		{{ form_start(form)}}
		{{form_widget(form)}}
		<button type="submit" class="btn btn-success">Enregistrer</button>
		{{form_end(form)}}

	</div>
{% endblock %}
